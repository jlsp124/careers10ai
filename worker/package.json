{
  "name": "careers10ai-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:schema": "wrangler d1 execute careers10ai-db --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260529.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.95.0"
  }
}
